const userId = "BlackPink";
const userInput = "BlacKPink";
if(userId.toLowerCase()==userInput.toLowerCase())
{
    console.log("The user is authorized User");
}
else
{
    console.log("We don't Know the user Sorry....");
}